version: "2"

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true
  go: '1.25'

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - ineffassign

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
